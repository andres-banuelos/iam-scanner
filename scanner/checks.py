"""\nscanner/checks.py\nAll IAM misconfiguration check functions.\nEach check returns a list of Finding objects.\nRead-only: only uses boto3 describe/list/get calls.\n"""\n\nimport boto3\nfrom datetime import datetime, timezone\nfrom typing import Optional\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Finding:\n    """Represents a single IAM misconfiguration finding."""\n    check_id: str\n    title: str\n    severity: str          # High / Medium / Low\n    resource: str          # e.g., username or policy ARN\n    description: str       # Technical description\n    audit_context: str     # Why this matters in a SOX audit\n    remediation: str       # Plain-English fix (1-2 sentences)\n    sox_domain: str        # SOX ITGC domain\n    nist_controls: list = field(default_factory=list)  # NIST 800-53 control IDs\n    raw_data: dict = field(default_factory=dict)       # Supporting evidence\n\n\ndef _age_days(dt: datetime) -> int:\n    """Return age of a datetime in days from now (UTC)."""\n    now = datetime.now(timezone.utc)\n    if dt.tzinfo is None:\n        dt = dt.replace(tzinfo=timezone.utc)\n    return (now - dt).days\n\n\n# ---------------------------------------------------------------------------\n# CHECK 1 — IAM users with no MFA enabled\n# ---------------------------------------------------------------------------\ndef check_mfa_disabled(iam_client) -> list[Finding]:\n    """Detect IAM users who have no MFA device enrolled."""\n    findings = []\n    paginator = iam_client.get_paginator(\"list_users\")\n    for page in paginator.paginate():\n        for user in page[\"Users\"]:\n            username = user[\"UserName\"]\n            mfa_devices = iam_client.list_mfa_devices(UserName=username)[\"MFADevices\"]\n            if not mfa_devices:\n                findings.append(Finding(\n                    check_id=\"IAM-001\",\n                    title=\"MFA Not Enabled\",\n                    severity=\"High\",\n                    resource=username,\n                    description=f\"IAM user '{username}' has no MFA device enrolled.\",\n                    audit_context=(\n                        \"SOX ITGC requires that access to systems processing financial data be \"\n                        \"protected by multi-factor authentication. A user without MFA represents \"\n                        \"a single point of failure: one compromised password grants full access. \"\n                        \"Auditors flag this as a deficiency in the Logical Access control domain.\"\n                    ),\n                    remediation=(\n                        \"Enroll a virtual MFA device (e.g., Google Authenticator) or hardware \"\n                        \"token for this user via the IAM console. Consider enforcing MFA via an \"\n                        \"IAM policy condition 'aws:MultiFactorAuthPresent': 'true'.\"\n                    ),\n                    sox_domain=\"Logical Access\",\n                    nist_controls=[\"IA-2\", \"IA-2(1)\", \"IA-5\"],\n                    raw_data={\"user_arn\": user.get(\"Arn\"), \"create_date\": str(user.get(\"CreateDate\"))}\n                ))\n    return findings\n\n\n# ---------------------------------------------------------------------------\n# CHECK 2 — Access keys older than 90 days (stale credentials)\n# ---------------------------------------------------------------------------\ndef check_stale_access_keys(iam_client, threshold_days: int = 90) -> list[Finding]:\n    \"\"\"Detect access keys that were created more than threshold_days ago.\"\"\"\n    findings = []\n    paginator = iam_client.get_paginator(\"list_users\")\n    for page in paginator.paginate():\n        for user in page[\"Users\"]:\n            username = user[\"UserName\"]\n            keys = iam_client.list_access_keys(UserName=username)[\"AccessKeyMetadata\"]\n            for key in keys:\n                age = _age_days(key[\"CreateDate\"])\n                if age > threshold_days:\n                    findings.append(Finding(\n                        check_id=\"IAM-002\",\n                        title=\"Stale Access Key (>90 Days Old)\",\n                        severity=\"High\",\n                        resource=f\"{username} / {key['AccessKeyId']}\",\n                        description=(\n                            f\"Access key '{key['AccessKeyId']}' for user '{username}' \"\n                            f\"was created {age} days ago and has not been rotated.\"\n                        ),\n                        audit_context=(\n                            \"SOX ITGC and most security frameworks require periodic rotation of \"\n                            \"credentials. Long-lived access keys increase the blast radius of a \"\n                            \"credential leak — an attacker with an old key may have maintained \"\n                            \"persistent access undetected. This violates the principle of \"\n                            \"credential hygiene central to the Access Management control domain.\"\n                        ),\n                        remediation=(\n                            f\"Rotate the access key by creating a new key, updating all systems \"\n                            f\"that use it, then deactivating and deleting the old key. \"\n                            f\"Automate rotation with AWS Secrets Manager or enforce via a \"\n                            f\"Service Control Policy.\"\n                        ),\n                        sox_domain=\"Logical Access\",\n                        nist_controls=[\"IA-5\", \"IA-5(1)\", \"AC-2\"],\n                        raw_data={\n                            \"key_id\": key[\"AccessKeyId\"],\n                            \"status\": key[\"Status\"],\n                            \"create_date\": str(key[\"CreateDate\"]),\n                            \"age_days\": age\n                        }\n                    ))\n    return findings\n\n\n# ---------------------------------------------------------------------------\n# CHECK 3 — Unused access keys (last used > 90 days or never used)\n# ---------------------------------------------------------------------------\ndef check_unused_access_keys(iam_client, threshold_days: int = 90) -> list[Finding]:\n    \"\"\"Detect access keys not used in the past threshold_days days.\"\"\"\n    findings = []\n    paginator = iam_client.get_paginator(\"list_users\")\n    for page in paginator.paginate():\n        for user in page[\"Users\"]:\n            username = user[\"UserName\"]\n            keys = iam_client.list_access_keys(UserName=username)[\"AccessKeyMetadata\"]\n            for key in keys:\n                if key[\"Status\"] != \"Active\":\n                    continue\n                last_used_resp = iam_client.get_access_key_last_used(AccessKeyId=key[\"AccessKeyId\"])\n                last_used = last_used_resp.get(\"AccessKeyLastUsed\", {})\n                last_used_date: Optional[datetime] = last_used.get(\"LastUsedDate\")\n\n                if last_used_date is None:\n                    label = \"never used\"\n                    age = 9999\n                else:\n                    age = _age_days(last_used_date)\n                    label = f\"last used {age} days ago\"\n\n                if last_used_date is None or age > threshold_days:\n                    findings.append(Finding(\n                        check_id=\"IAM-003\",\n                        title=\"Unused Access Key\",\n                        severity=\"Medium\",\n                        resource=f\"{username} / {key['AccessKeyId']}\",\n                        description=(\n                            f\"Active access key '{key['AccessKeyId']}' for '{username}' \"\n                            f\"is {label}.\"\n                        ),\n                        audit_context=(\n                            \"Unused credentials are a classic audit finding under SOX access \"\n                            \"reviews. They indicate orphaned access that was provisioned but never \"\n                            \"reclaimed — a sign of weak joiners/movers/leavers processes. \"\n                            \"An unused active key is an open door that offers no business value.\"\n                        ),\n                        remediation=(\n                            \"Deactivate keys unused for 90+ days and delete them after a short \"\n                            \"grace period. If the key belongs to a service account, confirm the \"\n                            \"service is still in use; if decommissioned, delete the user entirely.\"\n                        ),\n                        sox_domain=\"Logical Access\",\n                        nist_controls=[\"AC-2\", \"AC-2(3)\", \"IA-4\"],\n                        raw_data={\n                            \"key_id\": key[\"AccessKeyId\"],\n                            \"last_used_date\": str(last_used_date),\n                            \"last_used_service\": last_used.get(\"ServiceName\", \"N/A\"),\n                            \"age_days\": age\n                        }\n                    ))\n    return findings\n\n\n# ---------------------------------------------------------------------------\n# CHECK 4 — IAM policies with wildcard *:* permissions\n# ---------------------------------------------------------------------------\ndef check_wildcard_policies(iam_client) -> list[Finding]:\n    \"\"\"Detect customer-managed policies that grant Action:* on Resource:*.\"\"\"\n    import json\n    findings = []\n    paginator = iam_client.get_paginator(\"list_policies\")\n    for page in paginator.paginate(Scope=\"Local\"):  # customer-managed only\n        for policy in page[\"Policies\"]:\n            arn = policy[\"Arn\"]\n            version_id = policy[\"DefaultVersionId\"]\n            doc = iam_client.get_policy_version(PolicyArn=arn, VersionId=version_id)\n            statements = doc[\"PolicyVersion\"][\"Document\"].get(\"Statement\", [])\n            if isinstance(statements, dict):\n                statements = [statements]\n            for stmt in statements:\n                if stmt.get(\"Effect\") != \"Allow\":\n                    continue\n                actions = stmt.get(\"Action\", [])\n                resources = stmt.get(\"Resource\", [])\n                if isinstance(actions, str):\n                    actions = [actions]\n                if isinstance(resources, str):\n                    resources = [resources]\n                if \"*\" in actions and \"*\" in resources:\n                    findings.append(Finding(\n                        check_id=\"IAM-004\",\n                        title=\"Wildcard (*:*) Policy Permission\",\n                        severity=\"High\",\n                        resource=arn,\n                        description=(\n                            f\"Policy '{policy['PolicyName']}' ({arn}) contains a statement \"\n                            f\"granting Allow on Action:* / Resource:*.\"\n                        ),\n                        audit_context=(\n                            \"Overly permissive policies are a critical SOX ITGC finding. \"\n                            \"A wildcard policy effectively grants administrator-level access, \"\n                            \"violating the principle of least privilege. Auditors will flag this \"\n                            \"as a Significant Deficiency (SD) or Material Weakness (MW) if it \"\n                            \"is attached to any user or role accessing financial systems. \"\n                            \"This finding directly maps to the NIST AC-6 (Least Privilege) control.\"\n                        ),\n                        remediation=(\n                            \"Replace the wildcard statement with explicit, scoped actions and \"\n                            \"resources. Use IAM Access Analyzer to auto-generate least-privilege \"\n                            \"policies based on actual CloudTrail usage.\"\n                        ),\n                        sox_domain=\"Security\",\n                        nist_controls=[\"AC-6\", \"AC-6(1)\", \"AC-6(2)\", \"AC-3\"],\n                        raw_data={\n                            \"policy_name\": policy[\"PolicyName\"],\n                            \"policy_arn\": arn,\n                            \"default_version\": version_id,\n                            \"attachment_count\": policy.get(\"AttachmentCount\", 0)\n                        }\n                    ))\n                    break\n    return findings\n\n\n# ---------------------------------------------------------------------------\n# CHECK 5 — IAM users with no activity in 90+ days\n# ---------------------------------------------------------------------------\ndef check_inactive_users(iam_client, threshold_days: int = 90) -> list[Finding]:\n    \"\"\"Detect IAM users who have not performed any activity in threshold_days days.\"\"\"\n    findings = []\n    paginator = iam_client.get_paginator(\"list_users\")\n    for page in paginator.paginate():\n        for user in page[\"Users\"]:\n            username = user[\"UserName\"]\n            last_used: Optional[datetime] = user.get(\"PasswordLastUsed\")\n            keys = iam_client.list_access_keys(UserName=username)[\"AccessKeyMetadata\"]\n\n            for key in keys:\n                info = iam_client.get_access_key_last_used(AccessKeyId=key[\"AccessKeyId\"])\n                key_last = info.get(\"AccessKeyLastUsed\", {}).get(\"LastUsedDate\")\n                if key_last:\n                    if last_used is None or key_last > last_used:\n                        last_used = key_last\n\n            if last_used is None:\n                age = 9999\n                label = \"never active\"\n            else:\n                age = _age_days(last_used)\n                label = f\"last active {age} days ago\"\n\n            if age > threshold_days:\n                findings.append(Finding(\n                    check_id=\"IAM-005\",\n                    title=\"Inactive IAM User (>90 Days)\",\n                    severity=\"Medium\",\n                    resource=username,\n                    description=(\n                        f\"IAM user '{username}' has had no console or API activity. \"\n                        f\"User is {label}.\"\n                    ),\n                    audit_context=(\n                        \"SOX ITGC requires periodic user access reviews (UARs) — typically \"\n                        \"quarterly for privileged accounts. Inactive accounts that remain \"\n                        \"active in the system represent unnecessary attack surface and indicate \"\n                        \"a failure in the leavers/movers process. Auditors will request \"\n                        \"evidence of access recertification for accounts like this.\"\n                    ),\n                    remediation=(\n                        \"Disable or delete the account after confirming with the account owner \"\n                        \"that it is no longer needed. Implement a quarterly access review process \"\n                        \"to systematically identify and revoke dormant accounts.\"\n                    ),\n                    sox_domain=\"Logical Access\",\n                    nist_controls=[\"AC-2\", \"AC-2(3)\", \"AC-2(4)\"],\n                    raw_data={\n                        \"user_arn\": user.get(\"Arn\"),\n                        \"create_date\": str(user.get(\"CreateDate\")),\n                        \"last_active\": str(last_used),\n                        \"age_days\": age\n                    }\n                ))\n    return findings\n\n\n# ---------------------------------------------------------------------------\n# CHECK 6 — Root account with active access keys\n# ---------------------------------------------------------------------------\ndef check_root_access_keys(iam_client) -> list[Finding]:\n    \"\"\"Detect whether the root account has active access keys via credential report.\"\"\"\n    import csv, io, time\n    findings = []\n    iam_client.generate_credential_report()\n    for _ in range(10):\n        resp = iam_client.get_credential_report()\n        if resp[\"GeneratedTime\"]:\n            break\n        time.sleep(2)\n    content = resp[\"Content\"].decode(\"utf-8\")\n    reader = csv.DictReader(io.StringIO(content))\n    for row in reader:\n        if row.get(\"user\") != \"<root_account>\":\n            continue\n        key1_active = row.get(\"access_key_1_active\", \"false\").lower() == \"true\"\n        key2_active = row.get(\"access_key_2_active\", \"false\").lower() == \"true\"\n        if key1_active or key2_active:\n            findings.append(Finding(\n                check_id=\"IAM-006\",\n                title=\"Root Account Has Active Access Keys\",\n                severity=\"High\",\n                resource=\"<root_account>\",\n                description=(\n                    \"The AWS root account has one or more active programmatic access keys. \"\n                    \"The root account has unrestricted access to all AWS services and cannot \"\n                    \"be constrained by IAM policies.\"\n                ),\n                audit_context=(\n                    \"AWS and every security framework (CIS, SOC 2, SOX) explicitly prohibit \"\n                    \"the use of root account access keys. Root credentials used programmatically \"\n                    \"cannot be scoped, logged with user context, or revoked without deleting them. \"\n                    \"If compromised, an attacker gains irrevocable control of the entire AWS account. \"\n                    \"This is an automatic High severity finding in any cloud security audit.\"\n                ),\n                remediation=(\n                    \"Immediately delete the root account access keys from the AWS console under \"\n                    \"My Security Credentials. Use an IAM admin user or a role with appropriate \"\n                    \"permissions for all programmatic operations. Enable root account MFA.\"\n                ),\n                sox_domain=\"Security\",\n                nist_controls=[\"AC-6\", \"AC-2\", \"IA-2(1)\", \"SC-28\"],\n                raw_data={\n                    \"access_key_1_active\": key1_active,\n                    \"access_key_2_active\": key2_active,\n                    \"mfa_active\": row.get(\"mfa_active\", \"false\")\n                }\n            ))\n        break\n    return findings\n\n\n# ---------------------------------------------------------------------------\n# CHECK 7 — Password policy weaknesses\n# ---------------------------------------------------------------------------\ndef check_password_policy(iam_client) -> list[Finding]:\n    \"\"\"Evaluate the account-level IAM password policy for weaknesses.\"\"\"\n    findings = []\n    try:\n        policy = iam_client.get_account_password_policy()[\"PasswordPolicy\"]\n    except iam_client.exceptions.NoSuchEntityException:\n        findings.append(Finding(\n            check_id=\"IAM-007\",\n            title=\"No IAM Password Policy Configured\",\n            severity=\"High\",\n            resource=\"Account Password Policy\",\n            description=\"No custom IAM password policy is set. AWS defaults are applied, which do not enforce complexity or rotation.\",\n            audit_context=(\n                \"The absence of a password policy is a textbook SOX ITGC deficiency. \"\n                \"Without a policy, users can set trivially guessable passwords and never \"\n                \"be forced to change them. SOX requires documented and enforced controls \"\n                \"over logical access — an unset password policy fails that requirement on its face.\"\n            ),\n            remediation=(\n                \"Set an account password policy with: minimum 14 characters, require uppercase, \"\n                \"lowercase, numbers, and symbols, maximum 90-day expiry, and prevent reuse of \"\n                \"the last 24 passwords.\"\n            ),\n            sox_domain=\"Security\",\n            nist_controls=[\"IA-5\", \"IA-5(1)\", \"AC-2\"],\n            raw_data={}\n        ))\n        return findings\n\n    weaknesses = []\n    min_length = policy.get(\"MinimumPasswordLength\", 0)\n    if min_length < 14:\n        weaknesses.append({\"issue\": f\"Minimum password length is {min_length} (recommended: 14+)\", \"severity\": \"Medium\", \"nist\": [\"IA-5(1)\"]})\n    if not policy.get(\"RequireUppercaseCharacters\", False):\n        weaknesses.append({\"issue\": \"Uppercase characters not required\", \"severity\": \"Low\", \"nist\": [\"IA-5(1)\"]})\n    if not policy.get(\"RequireLowercaseCharacters\", False):\n        weaknesses.append({\"issue\": \"Lowercase characters not required\", \"severity\": \"Low\", \"nist\": [\"IA-5(1)\"]})\n    if not policy.get(\"RequireNumbers\", False):\n        weaknesses.append({\"issue\": \"Numeric characters not required\", \"severity\": \"Low\", \"nist\": [\"IA-5(1)\"]})\n    if not policy.get(\"RequireSymbols\", False):\n        weaknesses.append({\"issue\": \"Symbol characters not required\", \"severity\": \"Low\", \"nist\": [\"IA-5(1)\"]})\n    max_age = policy.get(\"MaxPasswordAge\")\n    if max_age is None or max_age > 90:\n        label = f\"{max_age} days\" if max_age else \"no rotation\"\n        weaknesses.append({\"issue\": f\"Password rotation not enforced or set too long ({label}). Recommended: <=90 days.\", \"severity\": \"Medium\", \"nist\": [\"IA-5\", \"IA-5(1)\"]})\n    reuse = policy.get(\"PasswordReusePrevention\")\n    if reuse is None or reuse < 24:\n        label = str(reuse) if reuse else \"0\"\n        weaknesses.append({\"issue\": f\"Password reuse prevention is {label} (recommended: 24+)\", \"severity\": \"Low\", \"nist\": [\"IA-5(1)\"]})\n\n    if weaknesses:\n        nist_all = list({ctrl for w in weaknesses for ctrl in w[\"nist\"]})\n        max_sev = \"High\" if any(w[\"severity\"] == \"High\" for w in weaknesses) else \\\n                  \"Medium\" if any(w[\"severity\"] == \"Medium\" for w in weaknesses) else \"Low\"\n        issues_text = \"; \".join(w[\"issue\"] for w in weaknesses)\n        findings.append(Finding(\n            check_id=\"IAM-007\",\n            title=\"IAM Password Policy Weaknesses\",\n            severity=max_sev,\n            resource=\"Account Password Policy\",\n            description=f\"The following password policy weaknesses were identified: {issues_text}.\",\n            audit_context=(\n                \"Password policy controls are one of the first items an IT auditor checks during \"\n                \"a SOX ITGC assessment. NIST 800-53 IA-5(1) requires enforcement of minimum \"\n                \"password complexity, rotation, and history controls. Weaknesses here are \"\n                \"documented as audit findings and must be remediated with evidence.\"\n            ),\n            remediation=(\n                \"Update the IAM account password policy to: min 14 characters, require all \"\n                \"character classes, set max age to 90 days, prevent reuse of last 24 passwords. \"\n                \"These settings are available under IAM -> Account Settings.\"\n            ),\n            sox_domain=\"Security\",\n            nist_controls=nist_all,\n            raw_data={\"policy\": policy, \"weaknesses\": weaknesses}\n        ))\n    return findings\n\n\n# ---------------------------------------------------------------------------\n# Master runner\n# ---------------------------------------------------------------------------\ndef run_all_checks(session: boto3.Session) -> list[Finding]:\n    \"\"\"Run every IAM check and return a combined list of Finding objects.\"\"\"\n    iam = session.client(\"iam\")\n    all_findings = []\n    checks = [\n        (\"MFA Disabled\", check_mfa_disabled),\n        (\"Stale Access Keys\", check_stale_access_keys),\n        (\"Unused Access Keys\", check_unused_access_keys),\n        (\"Wildcard Policies\", check_wildcard_policies),\n        (\"Inactive Users\", check_inactive_users),\n        (\"Root Access Keys\", check_root_access_keys),\n        (\"Password Policy\", check_password_policy),\n    ]\n    for name, fn in checks:\n        try:\n            results = fn(iam)\n            all_findings.extend(results)\n        except Exception as e:\n            print(f\"[WARN] Check '{name}' failed: {e}\")\n    return all_findings\n